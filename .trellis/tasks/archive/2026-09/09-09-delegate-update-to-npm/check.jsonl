{"file": ".trellis/spec/backend/index.md", "reason": "Check the layer rules after the rewrite: test/layering.test.ts must still pass, and src/cli/commands/selfUpdate.ts must contain no node:fs and no buildUrl. The install operation belongs in core, so verify the boundary held."}
{"file": ".trellis/spec/backend/error-handling.md", "reason": "Verify the rollback invariant survived the deletion. Removing restoreBackup must not remove the rule 'previous state destroyed only after the replacement is proven' — in the npm design that rule is expressed as the post-install version re-check. Confirm the three failure modes each fail loudly."}
{"file": ".trellis/spec/backend/quality-guidelines.md", "reason": "Confirm no runtime dependency was added (frozen list stays commander + picocolors) and that the tsup noExternal + createRequire banner is still present in tsup.config.ts. Removing it would restore ERR_MODULE_NOT_FOUND inside node_modules."}
{"file": ".trellis/spec/guides/versioning.md", "reason": "Verify package.json and src/version.ts agree and that the chosen bump is justified by an actual user-visible change, not habit. test/version.test.ts asserts the agreement."}
{"file": ".trellis/spec/guides/index.md", "reason": "Pre-Modification Rule: every deletion in steps 5 and 7 must be backed by a search proving no remaining consumer. Check that installDir, binShimPath, detectArch, detectPlatform, install:cli and package:release left no dangling references."}
{"file": ".trellis/spec/backend/directory-structure.md", "reason": "Confirm the deleted files (scripts/install.mjs, scripts/package-release.ts, src/core/zip.ts) left the directory structure coherent and that no orphaned script or test references them."}
{"file": ".trellis/spec/backend/logging-guidelines.md", "reason": "Check that --json mode still emits JSON only on stdout with npm diagnostics on stderr, and that a plain (non-json) run still shows npm's progress to the user."}
{"file": ".trellis/tasks/09-09-delegate-update-to-npm/prd.md", "reason": "Walk all 15 acceptance criteria against the diff. Two are the ones most likely to be asserted but not actually true: --check-only working with no npm present, and syncSkills reading from the package directory rather than a deleted install directory."}
{"file": ".trellis/tasks/09-09-delegate-update-to-npm/design.md", "reason": "Re-read section 2 (the invariant) and section 9 (what is not touched) and confirm neither drifted. Section 9 is the guardrail against re-breaking the shipped bundled-deps fix."}
{"file": ".trellis/tasks/09-09-delegate-update-to-npm/implement.md", "reason": "Run the Validation Commands block verbatim, including the npm pack smoke. That last block is the only guard on the thing this task must not change; if it fails, the tsup config was damaged."}
