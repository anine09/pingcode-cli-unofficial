{"file": ".trellis/spec/backend/quality-guidelines.md", "reason": "Runtime deps frozen at commander + picocolors — no new dependency may be added for the npm invocation. Also records this session's lessons: CI must smoke the packed artifact, a green push is not proof a workflow starts, and every CI step touching the remote must be idempotent (binds the release.yml zip removal in step 7)."}
{"file": ".trellis/spec/backend/error-handling.md", "reason": "Records the rollback invariant added this session (previous state destroyed only after the replacement is proven; a restore that cannot fail loudly is not a rollback). Deleting verifyBundle/restoreBackup must not delete that invariant — it becomes the reason the post-install version re-check exists in step 1."}
{"file": ".trellis/spec/backend/index.md", "reason": "Layer rules cli -> {api, core}, api -> core, core imports neither. Binding because the whole install operation moves into core: test/layering.test.ts fails if src/cli/commands/selfUpdate.ts contains node:fs or buildUrl. Also the --json stdout-only contract for the three failure messages in step 3."}
{"file": ".trellis/spec/backend/directory-structure.md", "reason": "Defines which layer owns filesystem and process-spawning work. Needed to place resolveNpm/installViaNpm correctly and to justify deleting scripts/install.mjs, scripts/package-release.ts and src/core/zip.ts without leaving an orphan."}
{"file": ".trellis/spec/guides/versioning.md", "reason": "Versioning is a rule, not a suggestion. Removing a documented install path (install.mjs + release zips) is user-visible, so step 8 proposes 2.0.0 rather than a PATCH — this spec is what the number is justified against."}
{"file": ".trellis/spec/guides/index.md", "reason": "Pre-Modification Rule is CRITICAL: search before deleting any value. Steps 5 and 7 delete installDir, binShimPath, detectArch, detectPlatform, the zip CI steps and their test assertions — each deletion must be preceded by a grep for remaining consumers."}
{"file": ".trellis/spec/guides/code-reuse-thinking-guide.md", "reason": "The task's entire premise: npm install -g already implements download, extract, in-place replacement and rollback. This guide is the check that the rewrite replaces code rather than duplicating it."}
{"file": ".trellis/spec/guides/commit-conventions.md", "reason": "Commit format type(scope): subject, one logical change, green tree. The rewrite spans 8 steps across two lanes, so it will need more than one commit."}
{"file": ".trellis/spec/guides/cross-layer-thinking-guide.md", "reason": "The change crosses cli and core and removes a scripts/ entry point. Needed to reason about the boundary between the version-check path (HTTP, stays) and the install path (npm, new)."}
{"file": ".trellis/spec/backend/logging-guidelines.md", "reason": "npm's own progress output must reach the user's terminal while --json keeps stdout JSON-only. Governs how installViaNpm forwards stdout/stderr and where diagnostics go."}
