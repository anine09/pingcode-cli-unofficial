{"file": "test/help.test.ts", "reason": "Today it hard-codes the group array, the exhaustive ordered 55-leaf list, and the 5 groups / 10 subgroups / 55 leaves arithmetic. F1 splits it into test/help/root.test.ts + test/help/<group>.test.ts (design D6.3); afterwards root asserts group order as GROUPS.map(...) and **no file anywhere may contain a global exhaustive leaf list**. It also carries the SKILL.md cross-check whose reverse direction (every leaf must be mentioned) is deliberately deleted per PRD R6 — verify it is the reverse, not the forward, direction that went away."}
{"file": "test/__snapshots__/help.test.ts.snap", "reason": "One 17.6 KB file holding the root, 5 group, 10 subgroup and 7 command --help snapshots. F1 deletes it and redistributes the content into test/help/__snapshots__/<group>.test.ts.snap so every module child owns its own snapshot file. Snapshot churn is this task's number one waste source (design D9 risk 1): review the redistribution as a pure move, and never regenerate with -u without reading the diff."}
{"file": "test/layering.test.ts", "reason": "Enforces cli->{api,core}, api->core, core->neither, api never imports output, cli never imports node:fs nor uses buildUrl. F2 must add exactly one rule: catalog.generated.ts may be imported only by core/catalog/index.ts. The existing invariants must not be relaxed (PRD A4) — the new core/catalog/ and core/metadata/ directories are additions, not exceptions."}
{"file": "test/metadata.test.ts", "reason": "419 lines of pjm resolver tests. F4's hard acceptance is that this file passes **UNMODIFIED** after core/metadata.ts becomes core/metadata/{registry,resolve,index}.ts — the re-export from index.ts is what keeps every import path unchanged. If a single assertion needs editing, F4 changed behaviour and must be reverted rather than patched."}
{"file": "test/shipMetadata.test.ts", "reason": "538 lines covering the productScoped resolvers and the shared loadSuiteTree that F4's RESOLVERS table must reproduce exactly, including the cross-branch suite-name ambiguity error. Must pass **UNMODIFIED** for the same reason as metadata.test.ts."}
{"file": "test/testhubMetadata.test.ts", "reason": "720 lines covering library-scoped cache keys and the two deliberate exceptions (testhub-library is the unparented bootstrap hop; testhub-case-important-level is org-level). It is the largest single proof that F4's table-driven MetaKind preserved every parent-scoping rule, so it must pass **UNMODIFIED**."}
{"file": "test/http.test.ts", "reason": "571 lines pinning the transport policy F3 inherits for free: the dry-run gate that never sends a mutating request while reads still run, exactly one 401 replay that cannot recurse, 429 honouring x-pc-retry-after with a 60 s cap and failing fast without the header, and any-2xx-is-success. core/http.ts is a forbidden file (PRD R1), so this suite passing unmodified is the evidence for design D3.5's claim that the generic executor adds zero new failure modes."}
{"file": "test/paginate.test.ts", "reason": "The query-string pagination flavour: 0-based page_index, cap 100, --all dedupe by id, stop on a short page, echoed-index bail-out. F3 selects it from the catalog's paged:'query' field instead of reimplementing paging, so this behaviour must not change."}
{"file": "test/searchPaginate.test.ts", "reason": "The POST .../search flavour, where paging moves into payload.page_size / payload.page_index. F3 selects it via paged:'search' for the five search endpoints; same no-behaviour-change requirement as paginate.test.ts."}
{"file": "test/api.test.ts", "reason": "pjm api-layer wrappers driven by an injected fetch. F1 moves api/parse.ts into api/parse/<module>.ts behind a full re-export, so this suite must pass unmodified — it is the cheapest available proof that the re-export is transparent and that no call site needed editing."}
{"file": "test/ship.test.ts", "reason": "Ship api-layer tests. Same F1 re-export transparency requirement, and S4 adds this file's cases for the five requirement-schedule / idea transition-history wrappers."}
{"file": "test/testhub.test.ts", "reason": "768 lines of testhub api-layer tests. Same F1 transparency requirement; S3 then adds cases for the bulk, history and plan-update wrappers, asserting method, path, query and body per wrapper exactly as the file already does."}
{"file": "test/commands.test.ts", "reason": "pjm command-layer tests. Must pass unmodified after F5 mounts the crosscutting subgroups onto project work-item and after any change to cli/commands/common.ts; S2b then adds its own cases for bulk-update, delete, link, tag, history and the two meta lookups."}
{"file": "test/shipCommands.test.ts", "reason": "872 lines of ship command tests exercising the shared runWrite / parseSetFlags / printPage plumbing. Must pass unmodified when F5 mounts relations/comments/attachments onto idea and ticket; S4 adds cases for its new leaves."}
{"file": "test/testhubCommands.test.ts", "reason": "1813 lines. S3 splits cli/commands/testhub.ts (1845 lines) into cli/commands/testhub/*.ts, and that commit must be behaviour-neutral — this suite has to pass unmodified across the split, with only the separate new-leaf commit adding cases."}
{"file": "test/workflows.test.ts", "reason": "Asserts that every `npm run <script>` referenced by .github/workflows/*.yml exists in package.json, plus the least-privilege grants — deliberately text-level, because adding a YAML parser would break the zero-new-dependency rule. F2 adds catalog:sync / catalog:check scripts and X2 adds a scheduled workflow, so this suite must be extended in the same commit or CI fails on a script-name typo."}
{"file": "skills/pingcode/SKILL.md", "reason": "The agent-facing contract. F1 rewrites it per PRD R6: keep the one-way assertion (every `pingcode ...` path it mentions must resolve in the commander tree), drop the exhaustive reverse assertion, and move module prose into skills/pingcode/modules/<module>.md so each child edits only its own file. It must document the api / api list / api describe escape hatch, that --json is a no-op under api, and that all 10 PUT endpoints are generic-layer only."}
{"file": "README.md", "reason": "User docs. X1 adds the two-layer model (reachable 459 / refined ~150), the per-module refinement coverage table, the three-set balance 53 + 107 + 299 = 459, and an explicit answer to 'why is there no scm platform replace' (every PUT is excluded by design D8.4). Its scope paragraph at lines 8-10 is also the citation the PRD uses to exclude wiki, so the two must stay consistent."}
{"file": ".github/workflows/ci.yml", "reason": "Node 20/22/24 x npm ci -> typecheck -> test -> build -> built-binary smoke, plus a separate hygiene job running scan:secrets and check:commits over the pushed range. catalog:check must **not** be added here: it needs the network, and as a PR gate an unrelated upstream change would block every merge — X2 adds it as a weekly scheduled workflow instead (design D2.5). Note the skill-installer step is skipped on Node 20 on purpose."}
