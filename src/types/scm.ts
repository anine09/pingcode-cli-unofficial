/**
 * scm / build / release (DevOps 数据集成) resource types — research §3.12.
 *
 * **Reserved by F1, deliberately empty.** S1a–S1d adds its types here, and only
 * here: `src/types/api.ts` already re-exports this module, so a new module's types
 * reach every existing `from '../types/api'` import without touching a shared file
 * (design D6.5/D6.6).
 */
export {};
